version: 2.1
jobs:
  test:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run:
          name: "Run tests"
          command: make test

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
